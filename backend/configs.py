# Ported from othello-engine's configs.py: everything there besides BOARD_SIZE
# was pygame GUI rendering constants (tile pixel sizes, colors, window dims),
# not used by the board/engine logic this backend actually serves.
BOARD_SIZE = 8
